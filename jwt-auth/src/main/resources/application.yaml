spring:
  application:
    name: jwt-auth

  datasource:
    url: jdbc:sqlserver://localhost:1434;databaseName=jwt_auth_db;encrypt=true;trustServerCertificate=true;loginTimeout=30;
    username: sa
    password: Bilal@123
    driver-class-name: com.microsoft.sqlserver.jdbc.SQLServerDriver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

server:
  port: 8083


